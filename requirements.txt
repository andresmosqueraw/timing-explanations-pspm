# Reference versions are the ones the committed result JSONs (fidelity_results.json,
# gain_table_results.json) were produced with (Python 3.10).
stable-baselines3==2.9.0
torch>=2.4
gymnasium==1.3.0
pandas>=2.0
numpy<2
pytest
captum>=0.9.0
scikit-learn>=1.3  # required by captum's KernelShap/Lime (linear surrogate fitting)
