__pycache__/
.pytest_cache/
.venv/
*.pyc
*.pyo
.env

# Heavy data/results, hosted elsewhere (kept on disk, not versioned here).
# PPO checkpoints under models/ and simbank_resources/models/ stay tracked
# (small, ~100-150KB each) -- only the raw/intermediate data is excluded.
PeerJ2023/
simbank_resources/data/
data/*.csv
data/*.parquet
data/loan_log_*
figures/out/
zenodo_upload/
catboost_info/
*.log
!models/**/*.log
models/variants/sweep/

# local snapshots taken before large regenerations
archive/
models/variants/sweep_oof/
